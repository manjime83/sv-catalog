import type { GatsbyConfig } from "gatsby";

const config: GatsbyConfig = {
  siteMetadata: {
    siteUrl: `https://sv.nimbusit.us`,
  },
  graphqlTypegen: true,
  plugins: [],
};

export default config;
